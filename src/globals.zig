pub var screen_w: f32 = 1600.0;
pub var screen_h: f32 = 900.0;

/// How many units does a tile take up (at default zoom, a unit is a
/// pixel)
/// A tile is a single 2x2 block
pub const tile_size: f32 = 32.0;
